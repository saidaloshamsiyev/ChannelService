server:
  port: 8085

spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 50MB
  application:
    name: CHANNEL-SERVICE
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/metube
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:1234}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect




eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URI:http://${EUREKA_HOST:localhost}:8761/eureka/}
  instance:
    preferIpAddress: true
#springdoc:
#  swagger-ui:
#    config-url: /api/channel/v3/api-docs
#    path: /api/channel/swagger-ui
springdoc:
  swagger-ui:
    path: /api/channel/swagger-ui
  api-docs:
    path: /api/channel/v3/api-docs
logging:
  level:
    org.springframework.web: DEBUG
    org.springframework.security: DEBUG


cloud:
  aws:
    credentials:
      accessKey: AKIA6GBMGGFRG54EE3UK
      secretKey: 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
    region:
      static: EU_NORTH_1
    s3:
      bucket: metube-bucket-mic


jwt:
  secret: qwefklsdfj3roif134adsj14fdslkjf1234fiodwq